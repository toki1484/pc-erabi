{
  "dependencies": {
    "astro": "^7.3.2"
  },
  "name": "pc-erabi",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "verify:data": "node scripts/verify-data.mjs",
    "deploy": "npm run build && wrangler deploy",
    "test": "node --test 'tests/*.test.mjs'",
    "fetch:requirements": "node scripts/fetch-requirements.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
